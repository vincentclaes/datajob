name: test-on-pr
on: pull_request

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: checking out
        uses: actions/checkout@v2
        with:
          ref: ${{ github.ref }}
      - name: install
        run: |
          npm install -g aws-cdk
          sudo -H pip3 install --upgrade poetry
          poetry install
      - name: tests
        run: poetry run python -m unittest
      - name: run example data_pipeline_simple
        run: |
          cd examples/data_pipeline_simple;
          poetry run datajob synthesize --stage stage --config datajob_stack.py;
      - name: run example data_pipeline_with_packaged_project
        run: |
          cd examples/data_pipeline_with_packaged_project;
          poetry run datajob synthesize --stage stage --config datajob_stack.py;
